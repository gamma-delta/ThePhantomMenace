modLoader = "javafml"
loaderVersion = "[43,)"
license = "MIT"
issueTrackerURL = "https://github.com/gamma-delta/ThePhantomMenace/issues"
[[mods]] #mandatory
# The modid of the mod
modId = "thephantommenace" #mandatory
version = "${file.jarVersion}" #mandatory
displayName = "The Phantom Menace" #mandatory
logoFile = "logo.png"
# A text field displayed in the mod UI
authors = "petrakat" #optional
credits = "naj77 for the logo"
# The description text for the mod (multi line!) (#mandatory)
description = '''
Make speed potions and glow berries (or whatever you like, for modpack makers) reset your sleep timer
'''

[[dependencies.thephantommenace]] #optional
# the modid of the dependency
modId = "forge" #mandatory
# Does this dependency have to exist - if not, ordering below must be specified
mandatory = true #mandatory
# The version range of the dependency
versionRange = "[43,)" #mandatory
# An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
ordering = "NONE"
# Side this dependency is applied on - BOTH, CLIENT or SERVER
side = "BOTH"

[[dependencies.thephantommenace]]
modId = "minecraft"
mandatory = true
versionRange = "[1.19.2,1.20)"
ordering = "NONE"
side = "BOTH"

[[dependencies.thephantommenace]]
modId = "paucal"
mandatory = true
versionRange = "[0.5.0,0.6.0)"
ordering = "NONE"
side = "BOTH"
